<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- =========================== -->
    <!-- PRODUCT TEMPLATE FORM EXT   -->
    <!-- =========================== -->
    <record id="product_template_form_view_nature" model="ir.ui.view">
        <field name="name">product.template.form.nature</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- Add nature fields after category -->
            <xpath expr="//field[@name='categ_id']" position="after">
                <field name="nature_id" placeholder="Sélectionner une nature..."/>
                <field name="nature_quantity" invisible="not nature_id"/>
            </xpath>
        </field>
    </record>

</odoo>
