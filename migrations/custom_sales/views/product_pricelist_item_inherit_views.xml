<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Vue formulaire -->
        <record id="view_pricelist_item_form_inherit" model="ir.ui.view">
            <field name="name">product.pricelist.item.form.inherit</field>
            <field name="model">product.pricelist.item</field>
            <field name="inherit_id" ref="product.product_pricelist_item_form_view"/>
            <field name="arch" type="xml">
                <field name="product_tmpl_id" position="after">
                    <field name="pricelist_id"/>
                </field>

                <!-- <field name="categ_id" position="after">
                    <field name="category_ids"
                           widget="many2many_tags"
                           options="{'no_create':1}"
                           invisible="display_applied_on != '2_product_category'"
                           placeholder="Toutes les catégories"
                    />
                </field>

                <field name="product_tmpl_id" position="attributes">
                    <attribute name="invisible">[('product_tmpl_ids', '!=', False)]</attribute>
                </field>

                <field name="categ_id" position="attributes">
                    <attribute name="invisible">[('categ_id', '!=', False)]</attribute>
                </field>

                <xpath expr="//group[@name='pricelist_rule_related']/field[@name='company_id']" position="before">
                    <field name="allowed_company_ids" widget="many2many_tags"/>
                </xpath>
                <xpath expr="//group[@name='pricelist_rule_related']/field[@name='company_id']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath> -->


            </field>
        </record>


    </data>
</odoo>